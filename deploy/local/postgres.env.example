# Local development only. Copy this file to postgres.env and choose a local
# password. Production credentials must come from the production secret store.
POSTGRES_DB=watchtrace_dev
POSTGRES_USER=watchtrace_dev
POSTGRES_PASSWORD=replace-with-a-local-only-password
